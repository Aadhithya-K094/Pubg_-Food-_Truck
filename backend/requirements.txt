# PUBG Food Truck - Backend (Django + DRF + MySQL) dependencies

# Core framework
Django==5.1.4

# REST API
djangorestframework==3.15.2

# MySQL database driver
mysqlclient==2.2.6

# CORS handling (so the React frontend can call the API)
django-cors-headers==4.6.0

# JWT authentication for the API
djangorestframework-simplejwt==5.3.1

# Read config/secrets from a .env file
python-dotenv==1.0.1

# Image handling (food truck / menu item images)
Pillow==11.0.0

# Production WSGI server (optional, for deployment)
gunicorn==23.0.0
