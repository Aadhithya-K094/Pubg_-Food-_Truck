# Copy to `.env` (or `.env.local`). Base URL of the Django API.
REACT_APP_API_URL=http://127.0.0.1:8000/api

# Google OAuth client ID (same value as backend GOOGLE_OAUTH_CLIENT_ID).
# Leave blank to hide the "Sign in with Google" button.
REACT_APP_GOOGLE_CLIENT_ID=
